# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (AWS RDS PostgreSQL)
# Format: postgresql://username:password@host:port/database
# Example: postgresql://chess_app:YOUR_SECURE_PASSWORD@chess-db.xxxx.us-east-1.rds.amazonaws.com:5432/chess_game
DATABASE_URL=postgresql://your_username:your_password@your-rds-endpoint.rds.amazonaws.com:5432/chess_game

# CORS Configuration (comma-separated URLs)
# Add your production frontend URLs (CloudFront, S3, or custom domain)
# Example: https://your-cloudfront-id.cloudfront.net,https://your-domain.com
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://your-cloudfront-distribution.cloudfront.net

# Optional: For future authentication features
# SESSION_SECRET=generate-a-strong-random-secret-key-here

# AWS Elastic Beanstalk Notes:
# - Set these as environment variables in EB console
# - Don't commit actual .env.production file
# - Use AWS Secrets Manager for sensitive data (optional)
